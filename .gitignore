# Bootstrap / launcher artifacts
.python/
.venv/
get-pip.py
python-embed.zip

# Python
__pycache__/
.ruff_cache/
*.py[cod]
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
dist/
build/
release/
# PyInstaller: dist/ and build/ (regenerated). Maintainer notes: docs/BUILD_AND_RELEASE.txt
# Tester handoff: release/ArloHub-Windows/ (built Install-*.exe when present).

# Editor / debug
.cursor/debug.log

# Logs and local data
arlohub_last_error.txt
arlo_shell_last_error.txt
arlo_logs/
*.log
debug-*.log
*.tar
.arlo_camera_config.json
.env

# Local FW server (user content)
local_fw_server/

# Claude Code worktrees / state (per-developer, never committed)
.claude/
