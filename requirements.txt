# Runtime + build dependencies.
# Floors (>=), not pins: CI installs the latest compatible release on every build,
# so the shipped app always tracks current library versions. Bump these floors when
# a newer minimum is required; hard-pin only if a release ever needs to be reproduced.
rich>=13.9.0
questionary>=2.1.0
paramiko>=3.5.0
requests>=2.32.0
pyserial>=3.5
PySide6>=6.8.0
# paramiko -> invoke -> yaml chain: pin pyyaml so PyInstaller bundles the full
# package (not just the _yaml C extension), preventing the
# "module 'yaml' has no attribute 'error'" startup crash on Windows builds.
pyyaml>=6.0.2

# --- Build / packaging (PyInstaller) ---
pyinstaller>=6.11.0
