Arlo Camera Control — developer notes
======================================

Graphical app (PySide6) for Arlo camera devices. Rich / questionary are still used
behind the scenes for shared command menus and prompts in the GUI.

Requirements
------------
- Python 3.10+ for local runs
- ADB: Android SDK platform-tools on PATH (optional)
- SSH: Paramiko only

Quick start (from repo root)
----------------------------
  pip install -r requirements.txt
  python main_gui.py

Device CLI commands load by product line: commands/command_profiles.json maps profile ids
to JSON files (E3 Wired → e3_wired_commands.json). commands/camera_models.py assigns
command_profile per device row. Unknown detected models get profile "none" (no device
catalog — only shared tools). See commands/HOW_TO_ADD_DEVICE_COMMANDS.txt to add lines.

Project layout
--------------
  main_gui.py — application entry point
  commands/camera_models.py — camera model groups and profiles
  connections/ — ADB, SSH, UART, connection_config
  commands/ — definitions and parser
  ui/ — menus, prompts, GUI
  utils/ — validators, logger

Windows installer (maintainers)
-------------------------------
See docs/BUILD_AND_RELEASE.txt and installer/README.txt.

License: internal hackathon demo.
